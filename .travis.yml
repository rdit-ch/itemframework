language: cpp

dist: trusty
sudo: required

before_install:
 - sudo add-apt-repository -y ppa:beineri/opt-qt58-trusty
 - sudo apt-get update -qq
 - sudo apt-get install -y qt58base qt58svg

script: 
 - source /opt/qt58/bin/qt58-env.sh
 - ./build.sh 

compiler:  clang


